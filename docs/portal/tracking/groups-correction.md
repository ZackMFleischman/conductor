User correction: replace All epic / parents with All parents. Selecting any parent must include the entire subtree, not only immediate children. CON-18 now uses complete ancestors ordered nearest parent to root, includes the selected ticket itself if present, and composes with search/assignee filters. Card labels show the root with the full path in a tooltip. The revised docs/portal/api-contract.md supersedes CON-19's initial epic/direct-parent proposal and asks CON-4 for complete access-safe ancestry. Fixtures demonstrate a grandchild with its intermediate parent absent from board rows. This does not claim live API integration.
Registry check during final verification returned REGISTRY_UNAVAILABLE inside the sandbox; installed CLI with authorized elevated access succeeded. Existing session and CON-18 claim reconciled; no fallback database created.
Final independent review caught the old epic/parent fields remaining in the JSON example after the prose changed. Corrected the example to the required nearest-to-root ancestors array; no code findings.
