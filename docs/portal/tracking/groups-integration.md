Proposal for CON-4 / coordinator-1 extending CON-13 portal contract review. Logical parent CON-8. User requested epic/parent filtering and compact epic membership. Frontend proposes explicit nullable epic and parent references, each {id,key,title}; epic is containing top-level epic, parent is immediate parent. Same reference may appear in both for a direct epic child. Null means known absence, not permission to infer ancestry from keys. Core owns hierarchy resolution and access-safe reference display. Please review updated docs/portal/api-contract.md on feat/portal with this proposal before any live adapter/service work. No schema migration or installation/API implementation authorized here. Fixture UI success is not integration acceptance.
