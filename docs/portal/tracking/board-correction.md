Independent review found an opaque-assignee-ID collision with all/unassigned filter values. Reproduced with a failing regression (3 cards vs expected 1), then tagged owner values separately; reviewer confirmed resolved. Full suite now 10 passed and production build passed. User's second density feedback removed the redundant fixture explanation and result-count label; controls now occupy a compact row. Final browser checks show board starts at y~145 desktop and y~177 at 390px; fixtures/read-only remain explicit. One Clear filters browser click did not act while user comments were arriving; after fresh page observation it restored all 8 correctly, and component regression also passes. No live API claim.
