# Corrected correction propagation evaluation

The coordinator dispatched a second fresh evaluator after freezing the corrected guidance and fixture hashes. This retry was justified by the first trial's specific invented-subcommand failure. Raw inputs, hashes, and response are retained under `.superpowers/remedies-20260913/con30-evaluation/trial2/` outside this source commit.

| Assertion | Result | Actual behavior |
| --- | --- | --- |
| Scoped npm instruction and provenance | Pass | Selected `READ-P5` and `READ-P10`, required first-affected-operation elevation for the exact cache, emitted no npm command because no subcommand was authorized, and expressly refused to infer `npm install`. |
| Exact-checkout Git instruction and provenance | Pass | Emitted both requested inspection commands with `core.excludesFile=`, exact `safe.directory`, matching `-C`, and `READ-P9` plus both supplied `CON-P38` sequence sources. |
| Installed Conductor instruction and provenance | Pass | Selected `CON-24`, exact executable/home and first-call elevation, emitted no invented subcommand, and preserved no-fallback/no-candidate limits. |
| Decoy exclusions | Pass | Excluded all five wrong-scope, invalidated, unverified, or expired records for their actual reasons. |
| Authority and mechanism limits | Pass | Kept corrections within the assigned operations and added no selector, database, tracker acknowledgement, session, persistent Git trust, wildcard, or indiscriminate elevation. |

Overall: pass (5/5). This is one observed guided selection from a mixed fixture. It does not make the prose workflow a deterministic matcher or establish permanent compliance.

The deterministic scorer ran against both raw responses. The original response exited 1 and reported the invented npm package operation; the corrected response exited 0 with exactly the three applicable selected IDs and all five excluded IDs. The corrected response artifact was first observed 51,722 ms after dispatch, within the 60-second artifact-production budget. Final message-delivery time was not captured.

The separate resource probe used `sandbox_permissions=require_escalated` against exactly `C:/Users/zFlei/AppData/Local/npm-cache`. Its first harness failed in 416.2738 ms before creating a file because the selected PowerShell cmdlet did not support `-LiteralPath`; that failure proves no access result and remains preserved. The corrected `FileStream` `CreateNew` probe created one GUID-named file, read the exact marker, and deleted that owned file. It exited 0; the measured operation ran from `2026-09-13T04:48:35.0811428Z` through `04:48:35.1929203Z` (104 ms internal, 396.5187 ms executor wall). No npm command, install, build, cache enumeration, unrelated deletion, fallback store, or live service operation occurred. This is corrected-retry scoped access evidence, not first-attempt success; an independent reviewer probe remains the planned first-attempt criterion check.
